# CantoDuplex:可本地部署的粤英双语 Duplex 交互决策层 — 方案 v1.1

> v1.1(2026-09-24):基于基线阶梯实测结果重写。相对 v1.0 的主要变化:
> ① 产品定位收敛为"本地部署 + 可挂接的音频交互决策层";
> ② 用 7 个基线的实测数据重新界定问题,方法重心从"应声"移到**说话对象**与**语调**;
> ③ 数据侧加入纠正分级(I0–I2)与语调最小对;④ 训练前先做可学性验证再投入大模型。
>
> 与 MPFD 学术线并行,互不挤占。

---

## 0. 一页纸

**做什么**:一个外挂在客户现有 ASR–LLM–TTS 系统旁的实时交互决策层。它只听音频、只输出控制信号(继续 / 停 / 暂停 / 应声 / 让出话轮 / 从某处恢复),不生成内容,可以在客户机房里的一张消费级 GPU 上运行。

**为谁做**:银行、保险、医疗、政务热线等**不能把客户通话音频发到境外云端**的机构。它们已经有自己的语音系统,需要的是能挂上去、能本地部署、决策可审计的组件,而不是换成 GPT-Live 或 Gemini Live。

**解决什么**(由实测确定,见 §1.3):所有可本地部署的现有方案,在两类情况上**全部失败**:
1. **说话对象**:用户在通话中转头跟身边的人说话(「阿媽幫我攞杯水」),现有方案 97–100% 会误以为是在对 agent 说话而停下;
2. **语调**:用户重复 agent 刚说的内容,升调「三百蚊?」是质疑(应停),降调「三百蚊。」是确认(应继续)。两句转写成文字完全一样,现有方案 93–100% 分不开。

外加一个工程底线:**电话信道下不能退化**。现有方案在 8k 窄带加噪声时明显变差,开源全双工模型甚至基本失效。

**核心判断**:应声、明显的纠正和抢话,用"VAD + ASR + 词表"的简单级联就能处理得不错。我们的价值不在这些地方,而在**把文字转写会丢掉的信息(说话方向、距离、语调)用回来**。

---

## 1. Motivation

### 1.1 市场:大模型越来越强,但银行用不了

- GPT-Live、Gemini Live、Qwen3.5-Omni 已经在做持续边听边说和语义打断,纯云端场景的交互体验会被它们迅速拉高;
- 但这些服务**只能通过境外云 API 调用**:Qwen3.5-Omni 没有开源权重(核实:HuggingFace 上 Qwen 官方仓库最新的 Omni 是 2025 年 9 月的 Qwen3-Omni),据了解 OpenAI 与 Gemini 的 API 也不向香港官方开放(待核实);
- 香港金融、医疗机构受数据合规约束(PDPO、金管局外包与数据出境要求),客户通话音频原则上不出机房,而且它们大多已经采购并部署了 ASR / LLM / TTS 供应商的方案。

**结论**:存在一个不会被一次大模型版本更新吃掉的市场:**本地部署、可审计、可挂接在现有系统上**的交互决策组件。在这个市场里,我们的对手不是 GPT-Live,而是能本地部署的开源方案和国内厂商的私有化方案。

### 1.2 技术:粤语的关键判别信息在转写之外

粤语是声调语言。粤港用户还大量句内夹用英语,并且常在嘈杂的家庭、街头环境中用手机或电话沟通。这带来两类文字无法承载的信息:
- **语调与声调**:「係啊」(确认)与「係咩」(质疑)、升调回声与降调回声,关键区别在音高轮廓上;
- **说话方向与距离**:对着手机说话和转头对旁人说话,在音量、混响、音色上都不一样,但转写结果可能完全正常。

主流做法是先转写成文字、再用规则或 LLM 判断,这等于在第一步就把这些信息丢掉了。

### 1.3 实测证据:基线阶梯

**评测设置**:380 条粤语探针。每条是 agent 正在讲一段客服话语(edge-tts 粤语合成),中途插入一条用户话语,标注了正确动作。开环评测:只记录系统会在何时做出何种决定。每个 session 只含一个探针,便于归因。评测代码与探针生成脚本都在仓库 `scripts/baseline/` 下。

**基线**(全部可本地部署;所有模型推理都在服务器上完成):

| 基线 | 类别 |
|---|---|
| ① VAD 一响就停 | 当前商用 server-VAD 的标准行为 |
| ② VAD + 流式 ASR + 应声词表(SenseVoice / Qwen3-ASR-1.7B) | 最强的"级联 + 规则"方案 |
| Freeze-Omni | 开源全双工模型(状态头) |
| SoulX-Duplug-0.6B | 开源可插拔语义 VAD(ASR 设为粤语) |
| MiniCPM-o 4.5 | 最新开源全双工模型(原生 duplex,1Hz 决策) |
| Qwen3-Omni-30B-A3B 作裁判 | 最强的可本地部署音频大模型,提示词中明确写出判断规则 |

**干净音频、"最终"视角**(不限反应速度,只看 agent 说话期间是否停下。continue 类为误停率,stop 类为漏停率,越低越好):

| 探针类别 | 期望 | ① VAD | ② +SenseVoice | ② +Qwen3-ASR | Freeze-Omni | SoulX-Duplug | MiniCPM-o 4.5* | Qwen3-Omni 裁判 |
|---|---|---|---|---|---|---|---|---|
| 应声「嗯嗯/係啊/okok」 | 继续 | 99% | **5%** | 24% | 47% | 88% | 62% | **7%** |
| **旁侧对话** | 继续 | 100% | **100%** | **100%** | 78%† | **100%** | 97% | **100%** |
| **确认回声「三百蚊。」** | 继续 | 100% | **100%** | **100%** | 64%† | **100%** | 93% | **100%** |
| 质疑回声「三百蚊?」 | 停 | 0% | 0% | 0% | 29% | 0% | 15% | 2% |
| 纠正 I0 / I1、抢话 | 停 | 0% | 0% | 0–5% | 17–35% | 0% | 10–30% | 0–6% |
| 停下的延迟中位 | | 0.25s | 0.37s | 0.77s | 2.3s | 0.33s | 2.7s | 0.88s |

\* MiniCPM-o 4.5 为部分结果(约 240/380),完整结果跑完后更新。
† Freeze-Omni 在这两类上数字较低,不是因为判断正确,而是因为它反应太慢(要等用户说完一整句才接话,中位延迟 2.3s),很多情况下没来得及停。

**电话信道**(8k 带通 + G.711 μ-law + 20dB 粉噪声;"及时"视角,≤0.8s 内停):
- 基线 ②(SenseVoice):应声误停 5% → **15%**;
- 基线 ②(Qwen3-ASR):漏停率 2% → **32%**,更强的 ASR 在电话信道下反而更差;
- Freeze-Omni:误停率 **98%**,反应延迟 0.31s,说明它在对噪声本身做反应,在电话信道下基本失效。

**从数据里读出的四个结论**:
1. **应声与明显的纠正、抢话,简单级联就能做好**(干净音频下 5% 误停、0% 漏停)。它们不是我们的主战场。
2. **说话对象与语调这两类,所有本地方案全部失败**,包括 30B 的音频大模型。即使在提示词里明确告诉 Qwen3-Omni"客户跟身边人讲话要继续、用降调重复是确认",它仍然对两类全部判 STOP。这说明问题不在"规则没讲清楚",而在**模型根本感知不到相应的信号**。
3. **全双工大模型(Freeze-Omni、MiniCPM-o)的行为几乎由"有没有人声、话语多长"决定,与内容关系不大**:听到人声就接话,且反应慢(2–3 秒)。
4. **电话信道是实际部署的分水岭**:级联方案明显退化,开源全双工模型失效。

**诚实的局限**:
- 探针由 TTS 合成(虽然已做语调质检),真人语音上的表现需要用真人录音确认(见 §3 第三步);
- 应声词表与探针措辞同源,基线 ② 的应声成绩偏乐观;在真实应声片段上的测试(测试 A)被串音混杂,没有得出干净结论;
- Qwen2-Audio-7B 也测过:它无法遵循"只输出 STOP/CONTINUE"的指令,强制二选一时又退化为永远 CONTINUE,因此没有列入主表。这本身说明,上一代 7B 音频大模型不经训练无法直接作为打断控制器使用。

完整结果:`docs/BASELINE_RESULTS_v2.md`(v1 探针版见 `docs/BASELINE_RESULTS_v0.md`)。

---

## 2. Method

### 2.1 问题定义

- **输入**(三路,均为流式):① 用户麦克风音频(经过或未经过客户的回声消除,需在接入时声明);② **自听通道**:agent 正在播放的文本与播放进度(由客户 TTS 侧提供,不需要重新编码音频);③ 可选的客户 ASR 流式转写。
- **输出**:每 80–160ms 一个控制动作:`CONTINUE` / `ATTENUATE`(压低音量试探)/ `PAUSE` / `CUT` / `BACKCHANNEL_ACK` / `YIELD` / `RESUME(position)`。
- **与客户 LLM 的接口**:我们只输出**恢复位置和结构化上下文补丁**(用户实际听到了哪里、哪个信息被质疑或纠正),不生成恢复话术;话术由客户自己的 LLM 生成。这样产品边界清楚,也不和客户的垂直 LLM 冲突。

### 2.2 系统架构:快慢双通路,按实测证据分工

```
用户音频 ──► 快通路(~100–300M 流式声学模型,40ms 级)
   │          VAD / 应声检测 / 明显打断 / 电话信道鲁棒
   │          只允许可逆动作:ATTENUATE、BACKCHANNEL_ACK
   │
   ├──► 慢通路(Qwen3-1.7B + 音频 adapter,事件触发,6–12Hz 重判定)
   │          专门处理两个难点:说话对象、语调
   │          不可逆动作 CUT / YIELD / PAUSE 必须由慢通路确认
   │
自听通道(agent 已播文本 + 进度)──► 慢通路
                                   │
                              仲裁层(可逆优先、2-tick 迟滞、行业阈值、全量日志)
```

相对 v1.0 的两处修正:
- **慢通路不再由快通路的应声判断来决定是否唤醒**,只要 VAD 在 agent 说话期间检测到人声就运行。否则以「係」开头的纠正会被快通路误判为应声,慢通路根本不会醒,错误无法挽回;
- **快通路的目标是"达到级联方案的水平,同时更快、在电话信道下不退化"**。基线已经说明这部分不难,不在它上面堆复杂度。

### 2.3 核心能力一:语调感知

基线显示,即使是 30B 的音频大模型也听不出升调与降调的区别。这和 v1.0 预判的"两个丢失点"一致:
- **丢失点①:通用音频编码器会弱化音高信息** → **韵律旁路**:从原始音频用 DSP 直接提取 log-F0、清浊、能量、语速变化(8 维 @25Hz),经小型 MLP 升维后与编码器输出逐帧拼接,绕过编码器;
- **丢失点②:adapter 在只有转写目标的训练中会把韵律维度压掉** → Stage A 多任务:转写 + **声调序列预测** + 语气分类,迫使投影层保留韵律;
- **韵律探针集**:200–300 条只差语调的最小对,每个训练阶段结束都必须测,韵律信息在哪一层丢失可以立刻看出来;
- 备选:韵律流单独投影、不经过语义 adapter(作为消融实验)。

### 2.4 核心能力二:说话对象判断

从 MPFD 的经验看,只靠文本判断说话对象有上限(AMI 上 F1 仅 0.68),所以要结合三类线索:
- **声学线索(主要)**:转头对旁人说话时,音量下降、离麦克风更远、混响增加(直达声与混响比下降)、音高和语速也会变。这些都在音频 embedding 和韵律旁路里;
- **内容相关性**:用户的话与 agent 正在讲的内容是否相关。慢通路的输入里有 agent 已播文本,可以直接判断;
- **称呼词与话题**:「阿媽」「細佬」、与通话无关的家务话题。

### 2.5 慢通路如何流式运行

- 流式因果编码器(~150M,初始化候选:Freeze-Omni 编码器 vs FunASR 粤语流式编码器,M1 小实验确定)→ 与韵律旁路逐帧拼接 → 2–4 倍下采样到 6–12.5Hz → 2 层 MLP adapter 投影到 LLM 维度(2048)→ 以 `inputs_embeds` 注入 Qwen3-1.7B;
- 序列布局:`<system>` 对话历史 → `<agent_playing>` 按播放进度逐词追加 → `<user>` 逐 chunk 追加音频 token(LID 翻转时插入 `<SW:yue→en>`)→ `<decide>` 解码 1 个受限动作 token;
- 对话历史与 agent 台词提前预填 KV cache,每次判定只增量处理新到的音频 token 并解码一个 token。单次判定预计 ≤125ms(4060 级 GPU,INT4)。

### 2.6 数据

| 数据 | 用途 | 状态 |
|---|---|---|
| **种子真集**(双通道真人录音,10h 起步,以 WoZ 为主) | 真实 timing、金标签、评测真轨 | 采集协议已写(`docs/COLLECTION_PROTOCOL.md`),待招募 |
| **真人小样**(每人 50–100 条:升/降调回声、电话中转头对旁人说话、自然应声) | **两类核心能力的真人评测**;测试 A 的干净重做 | **需要尽快安排**(§3 第三步) |
| 爬取语料(RTHK 烽煙 + YouTube,~100h) | 双域 timing 先验(已完成)、编码器预训练、应声词挖掘 | **版权语料只用于内部研究,不进入交付模型**(见 §4) |
| **合成数据**(编译器式流水线) | 策略层的主要监督来源 | ②③ 已完成;待:Writer LLM 扩充、CosyVoice2 渲染 |

**合成流水线的关键设计**(已实现并测试,`synth/`):
- 事件类别由代码注入,**标签由构造保证**;LLM 只负责措辞多样性,措辞必须通过类别不变量校验器;
- **纠正分级**:I0(带否定词,正则可解)/ I1(不带否定词,只说出新值,校验器禁止出现否定词)/ I2(回声最小对,文本相同、只靠语调);
- **槽位翻转**:纠正事件同时自动产出纠正语义、恢复位置、原本未说完的句子,解决了 RESUME 监督在自然数据里不存在的问题;
- 所有时间量从爬取语料的真实分布中采样,按域选择(客服域用电台烽煙分布,闲聊域用 YouTube 分布);
- **针对两类核心能力的新增数据(下一步)**:旁侧对话用"转头"声学模拟(降音量、房间混响 RIR、距离效应);回声最小对做句尾音高质检;全部数据加电话信道增广。

**评测与训练的 TTS 隔离**:评测探针用 edge-tts,训练数据用 CosyVoice2,防止模型学会分辨某个 TTS 的痕迹。

### 2.7 训练

| 阶段 | 训练对象 | 任务 | 数据 |
|---|---|---|---|
| **Stage 0 可学性验证**(新增) | 冻结编码器上的小分类器 | 说话对象二分类、升/降调二分类 | 针对性合成数据 + 韵律特征 |
| Stage A 模态对齐 | adapter + 编码器 LoRA(LLM 冻结) | 转写 + 声调序列 + 语气,多任务 | CS-ASR 数据 + 爬取语料伪标签 |
| Stage B 决策训练 | adapter + LLM LoRA | 因果 chunk 级控制 token | 合成 + 种子真集;单语粤:单语英:混语 ≈ 4:3:3 |
| Stage C 校准 | 阈值 + 少量偏好优化 | 误停与漏停代价不对称,按行业加权 | 真实困难样本小集 |

**两个容易做错的地方**:
- **因果标签**:每个时刻的监督只看截至当时的证据(纠正刚开口的 200ms 内标签是 ATTENUATE,证据充分后才升级为 CUT),禁止使用未来信息;
- **标签严重不平衡**:一个 session 里 CONTINUE 有几百个 chunk,CUT 只有个位数。只监督事件窗口内的 chunk,窗口外按比例抽样负例,再配合类别加权或 focal loss。

**防遗忘门禁**:单语粤语、单语英语回归集每次训练后必测,韵律探针集每阶段必测。

### 2.8 评测

评测 harness 已实现(`harness/` + `scripts/baseline/score.py`),同时服务三个用途:论文的 benchmark、自家回归测试、客户验收工具。
- 指标:误停率 × 漏停率二维图,反应延迟 p50/p90,恢复位置正确率;
- 两种视角:**及时**(≤0.8s 内停)与**最终**(说话期间是否停);只统计 agent 说话期间的停止;
- 评测条件:干净音频 / 电话信道 / 真实应声(+串音对照)/ **真人录音(待补)**。

---

## 3. 后续方案

### 第一步:两类核心能力的训练数据(服务器,约 2–3 天)
- 用合成流水线批量生成旁侧对话与回声最小对,在服务器上用 CosyVoice2 渲染粤语;
- 旁侧对话加"转头"声学处理;回声最小对做句尾音高对比质检;全部加电话信道增广。

### 第二步:Stage 0 可学性验证(服务器,约 1–2 天)——**决策关口**
- 在冻结的音频编码器特征 + 韵律特征上训练小分类器,只做"是否对 agent 说话"和"升调还是降调"两个判断;
- 在 edge-tts 评测探针上测试(训练与评测 TTS 不同,同时检验跨 TTS 泛化);
- **通过**(两类都明显优于随机、且跨 TTS 不崩):说明信号确实在音频里,进入第四步;
- **不通过**:先解决数据或特征问题,不进入大模型训练。

### 第三步:真人小样(**2026-09-24 决定后移**:先在合成数据上明显超过基线,再做真人验证,以控制成本)
- 采集材料包已准备好(`docs/human_sample_kit.zip`:说明、同意书、30 张语调情景卡、说话对象台词、客服讲解音频、每人随机顺序清单、标注表;所有文本与训练数据不重合),找人即可开始;
- 5 人,每人 A(语调)60 句 + B(说话对象)50 句 + C(自然应声)约 2 分钟,各自单独录音;
- 用途:两类核心能力的真人评测(防止"只学会了分辨 TTS"的质疑);测试 A(真实应声)的干净重做;**只用于评测,不用于训练**;
- 在此之前,合成评测保留三道隔离作为替代:跨 TTS(训练 CosyVoice3 / 评测 edge-tts)、跨房间仿真(合成指数衰减 RIR / pyroomacoustics 镜像法)、说话人隔离;
- 可选的低成本冒烟:用户本人录 A 的 10 张卡与 B 的 10 组同句两讲(约 40 条,10 分钟),在正式训练前检查模型是否只认合成语音。

### 第四步:慢通路正式训练(服务器,约 2–4 周)
- Stage A → Stage B → Stage C;
- 同步完成快通路(应声检测 + 电话信道鲁棒),目标达到并超过基线 ② 的水平。

### 第五步:Demo 与接入规范
- 三个 demo 场景重新排序:**① 转头对家人说话,agent 不停;② 用升调质疑一个数字,agent 立即停下确认;降调确认时 agent 继续;③ 电话信道下应声不打断**;
- 编写**接入规范**:客户需提供 TTS 播放控制(暂停/停止/调音量)、自听通道(播放文本与进度)、用户音频流;我们输出控制动作与上下文补丁。

### 论文
- **论文一(benchmark)**:粤英混语 duplex 交互评测。核心卖点是基线阶梯的发现:所有本地方案在说话对象与语调上全部失败;电话信道是分水岭。配合 I0–I2 分级、语调最小对、真人评测轨;
- **论文二(方法)**:韵律旁路 + 说话对象声学线索 + 因果监督 + 槽位翻转合成,以及消融实验。

### 里程碑

| 阶段 | 内容 |
|---|---|
| M1(当前) | 基线阶梯 ✅;第一、二步;真人小样;种子集招募启动 |
| M2 | 论文一投出;Stage A/B;快通路 |
| M3 | 三场景 demo;量化部署与接入件;集成商或客户试点;论文二 |

---

## 4. 风险与对策

| 风险 | 对策 |
|---|---|
| 两类核心能力在音频中信号不足、学不出来 | 第二步可学性验证作为决策关口,在投入大模型之前排除 |
| 模型学会分辨 TTS 痕迹而非真实线索 | 训练与评测 TTS 隔离;真人小样作为最终评测 |
| 合成的"转头"声学与真实情况差距大 | 真人小样校准;后续用种子集真实录音替换 |
| 大模型厂商推出可本地部署的强模型 | 基线阶梯持续更新(评测线本身就是资产);卖点落在本地部署、可审计、电话信道、粤语语调 |
| **交付模型的训练数据来源不可审计** | 爬取的版权语料只用于内部研究和 timing 统计,不进入交付模型的训练;交付模型只用自采数据、授权数据和合成数据,训练前确定这条边界 |
| 语音接入依赖客户系统的能力 | 接入规范中写明最低要求;不满足 TTS 播放控制的客户,只提供部分功能 |
| 服务器环境与磁盘 | 所有环境、模型、缓存放在 `/import/home2`;`/import/home` 共享盘曾写满,需持续关注 |

---

## 5. 已完成的资产(截至 2026-09-24)

代码仓库:https://github.com/Shelton1013/cs_duplex

- **评测**:harness(误停/漏停/延迟,两种视角);v1(300 条)与 v2(380 条,含 I2 语调最小对与音高质检)探针集;电话信道与真实应声两组鲁棒性测试;7 个基线的运行脚本与结果;
- **数据**:RTHK 与 YouTube 爬取管线(约 100h);diarization 与双域 timing 先验;
- **合成**:编译器式流水线 ②③(槽位翻转、I0–I2 分级、因果 chunk 标签,16 个测试通过);Writer LLM 客户端与服务器部署脚本;
- **文档**:种子集采集协议、环境配置、基线结果。

## 6. 待决定

1. **交付模型的训练数据边界**:哪些数据可以进入交付模型(建议:只用自采、授权与合成数据);
2. **真人小样的人手安排**:谁来录、录多少;
3. **慢通路编码器初始化**:Freeze-Omni 编码器 vs FunASR 粤语流式编码器(Stage A 前用小实验确定);
4. **商业 API 参照测试**(GPT-Live / Gemini Live / Qwen3.5-Omni):优先级低,作为"本地方案能达到云端水平的多少"的参照,之后再定是否做。
