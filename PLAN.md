# CantoDuplex:粤英双语 Duplex 交互决策控制器 — 完整方案

> 版本 v1.0(2026-09-17,待最终 check)
> 定位:产品向路线,与 MPFD 学术线(ICASSP 在途)并行,互不挤占

---

## 0. 一页纸总结

**做什么**:一个外挂式(sidecar)的实时交互决策控制器,插在客户现有 ASR–LLM–TTS 级联旁,每 80–160ms 输出一个控制决策(继续说 / 降音量试探 / 停 / 应声 / 让出话轮 / 从正确位置恢复),对 LLM 只暴露控制信号 + 上下文修正,不碰内容生成。架构上钉死在 C1(外挂学习控制器)位置。

**三大卖点(已定)**:
1. **语种长尾**:粤语 / 粤英句内混说,电话 8k 窄带,香港客服合规刚需(服务必须提供粤语);
2. **私有化 + 低算力**:单张消费级 GPU(4060/4090)跑 8–32 并发路,与客户级联同机部署,数据不出域;CPU 仅作降级模式;
3. **杀手 demo**:用户说"嗯嗯係啊"时,别人的 agent 停了,我们的流畅讲完;用户说"唔係啊"时,300ms 内停下并从正确内容点恢复。

**为什么是外挂而不是原生模型**:市场证据(survey 目录)显示有营收的商业系统全部停留在 T1 级联;客户换 LLM / 换 TTS / 换垂直领域模型都不影响控制器,解耦是卖点不是妥协;可审计(每个动作有日志、阈值可调)是金融/医疗客户的硬需求,黑盒原生模型给不了。

**护城河(四层叠加,随时间增厚)**:数据飞轮(部署后生产困难案例回流重训)> 评测即标准(CS-Duplex-Bench 定义"什么叫好")> 集成与切换成本(自听通道 / RESUME / 电话系统接入件 / 行业阈值 profile)> 合规信任(审计日志、私有化)。静态数据壁垒只领先 12–18 个月,关键是先转起飞轮。

---

## 1. 系统架构

### 1.1 输入(三路)

| 输入 | 说明 | 关键设计 |
|---|---|---|
| 用户麦克风音频流 | 16k 宽带 + 8k 电话窄带 | 决策通路**不走 streaming ASR**,直接音频 embedding(ASR partial 滞后 300–600ms,会爆延迟预算) |
| 自听通道 | agent 实际播出内容 | 不重编码音频:直接用 TTS 已知的合成文本 + 播放进度 + 能量包络(外挂形态白拿) |
| ASR partial(可选) | 客户 ASR 的流式部分转写 | 仅作滞后辅助流注入(词稳定一个补一个),不在决策关键路径 |

### 1.2 输出(事件流)

`CONTINUE` / `ATTENUATE`(降音量试探) / `PAUSE`(说完当前短语停) / `CUT`(立即停) / `BACKCHANNEL_ACK`(用户在应声,忽略) / `YIELD`(让出话轮) / `RESUME(position)`(从被打断处恢复)
附带:打断发生时,向 LLM 上下文写入**"用户实际听到的前缀截止到哪个词"**——恢复正确性的关键,也是 server-VAD 竞品完全没有的能力。

### 1.3 快慢双通路 + 仲裁

**快通路(声学,~40ms 反应,100–300M)**:因果流式 Conformer,帧级输出:语音活动、能量/音高轮廓、说话人方向(近讲/远场)、**封闭集粤英应声词检测头**(嗯嗯/係/啱/好/okok/yeah/right…,ASR 出字之前判定——demo 场景 1 的技术落点)。粤语部分利用声调轮廓("係"在确认与反问语境调型不同)。

**慢通路(语义决策,200–400ms 反应,1.5–3B 语音 LLM)**:事件触发,非逐帧调用——只在快通路报告模糊情况(agent 说话期间用户开口且非已知应声词)时被唤醒。判断:纠正 / 补充 / 抢话 / 旁侧对话 / 噪声 → 输出控制动作。混语语义判断("唔係啊 wait 我 mean 下个月")是 3B 与 300M 判别头的质变点,也是数据壁垒变现处。

**仲裁(可逆优先,几乎不训、只校准)**:快通路只有权做可逆动作(ATTENUATE / BACKCHANNEL_ACK);不可逆的 CUT / YIELD 必须慢通路确认,或快通路极高置信(如用户长时间连续说话)。配"连续 2 tick 判 CUT 才执行"迟滞。每级阈值可调、每个动作有日志 = 可审计卖点。按行业出厂 profile(银行档宁可多停 / 陪伴档多应声)。

### 1.4 慢通路细节:3B LLM 如何流式消费音频

**原理**:Transformer 不关心 embedding 来自词表查表还是外部投影(`inputs_embeds` 接口);音频经 adapter 投影后与文本 embedding 拼成混合序列即可。可行性先例:SLAM-ASR(线性层+冻结 LLM 即可做 ASR)、Qwen-Audio、Freeze-Omni、SALM-Duplex。我们做的是它们的**纯决策特化版**(不生成语音,只吐控制 token),任务更轻。

**模块与维度(底座已定 Qwen3-1.7B,hidden 恰好也是 2048,下图维度不变)**:

```
用户音频 80ms chunk
   │
流式因果编码器(~150M Conformer,512 维 @25Hz)   ← 初始化:FunASR/WeNet 粤语流式底座
   │                                              或 Freeze-Omni 开源流式编码器(勿用 Whisper,双向注意力改因果伤筋动骨)
   ├── 逐帧拼接 ◄── DSP 韵律旁路流:logF0/清浊/能量/语速Δ(8维@25Hz)→ 小MLP 升 64 维
   │   (512+64=576)
时间下采样 ×2~4(25Hz → 6.25~12.5Hz)             ← 一次打断片段 1–3s = 10–40 token,别保留 25Hz
   │
Adapter:2层 MLP 576×k → 2048(带 LayerNorm)
   │
= 音频 token(向量),直接注入 LLM 输入序列
```

**序列布局(时分复用三流模板)**:

```
<system> 你是交互决策器,输出动作 token …
<history> 近几轮对话文本 …
<agent_playing> 帮 你 查 到 下 个 月 …    ← 随 TTS 播放进度逐词追加(自听通道)
<user> [a1][a2] <SW:yue→en> [a3][a4] …    ← 用户开口后逐 chunk 追加;LID 翻转时插切换 token
<decide> → 解码 1 个控制 token
```

关键细节:
- agent 文本**按播放进度追加**,时间关系被序列顺序天然编码("用户在 agent 说到'下个月'时开口")——RESUME 恢复点的信息来源;
- 用户音频 token **由快通路 VAD 门控**(静音不进),省 token 且明确快慢通路接口;
- `<SW:…>` 与 `<CONTINUE>/<CUT>/…` 均为新增特殊 token;解码时**词表约束在十来个动作 token 内 argmax**,不自由生成(延迟压到 ~20ms 的原因);
- KV cache append-only:对话历史 + agent 已播词提前预填(agent 说话的 GPU 空闲期完成),每 tick 只增量 prefill 新到 token,历史零重算。

**延迟账(4060 级,原按 3B INT4 估计;底座已定 1.7B,只会更快)**:攒块 80ms + 编码 ~10ms + 增量 prefill ~15ms + 解码 1 token ~20ms ≈ **单次判定 ≤125ms**;2-tick 迟滞后不可逆动作 250–300ms;快通路 40ms 已先 ATTENUATE 兜底,用户无"卡顿"感知。慢通路以 6–12Hz 被反复重问"现在该干嘛"。

### 1.5 韵律与 code-switching 的两个丢失点(架构级保障)

```
音频 → 编码器 → [丢失点①] → 拼韵律旁路 → adapter → [丢失点②] → LLM
```
- **丢失点①(编码器内部弱化音高)**:韵律旁路流从原始音频 DSP 直提,不经编码器 → 保证**可用性**;
- **丢失点②(adapter 是被训练的瓶颈,ASR 目标不给韵律梯度就会把韵律维度实质置零)**:Stage A 多任务(见 §3)→ 保证**利用率**;
- 备选保险:韵律流独立小投影层、不走语义 adapter(Stage A 碰不到它,代价是要等 Stage B 才有梯度)——作为消融实验,若韵律探针显示主方案保不住敏感度则切换;
- **韵律探针集(项目开始即建)**:200–300 条只差声调/语气的最小对("係啊/係咩/唔係啊"),每训练阶段结束必跑,韵律信息死在哪一层立刻可见。

**切换点利用**:共享编码器挂流式 LID 头(帧级 粤/英/静音 后验;SwitchLingua 有词级语言标注可训)——后验翻转即切换点;置信超阈值时向 LLM 注入 `<SW:yue→en>` 事件 token(可解释:审计日志能写"检测到切英+音高重置,判定强调性纠正")。

---

## 2. 数据方案(护城河所在,人力投入 > 50%)

1. **种子真集(20–50h,双通道,精标 10h)**:SwitchLingua 采集协议升级双通道版;熟人粤语/混语通话 + 客服角色扮演(预约/查账单/改地址)。事件标签:{应声、纠正、抢话、协作性接话、旁侧对话、电视/环境人声、咳嗽/填充词}。对标 SmoothConv 定位——精标小数据做控制监督与评测,不追规模。**先做 10–20h 种子集验证 pipeline 再决定扩量。**
2. **合成扩量(500–2000h)**:Writer–Director 式(LLM 写带事件标记的混语剧本 → 粤语多说话人 TTS 渲染双通道);**timing 从种子真集分布采样**(gap/overlap/应声时点),不用规则偏移,避免合成指纹。
3. **困难负例库(demo 质量的决定因素)**:①应声词全表(粤/英/混);②真纠正最小对比对("係啊"继续 vs "唔係啊"要停);③**只差切换的最小对**("係啊係啊"=CONTINUE vs "係啊 but wait"=PAUSE vs "唔係 no no NEXT month"=CUT)——逼模型用上韵律×切换联合信号;④旁侧对话(用户转头对家人说粤语);⑤agent 自身回声残留。每类进评测集。
4. **电话信道增广**:8k 重采样、G.711/Opus 压缩、丢包、远场混响。
5. **RESUME 训练数据**:数据管线从第一天记录"实际播出前缀",构造(打断点 + 正确恢复文本)对——事后补不了标签。
5b. **网络挖掘辅助轨(2026-09-18 增)**:爬取真实语料作三个用途——①**timing 先验**(最有价值):香港电台/商台烽煙 phone-in 节目(真电话信道+真打断+粤语,与目标域同分布),只做 diarization 量 overlap/应声/打断时点分布喂合成 pipeline,不需要分离;②**编码器预训练/Stage A 增补**:YouTube 港式播客/访谈,ASR 式训练对混合通道不敏感;③**应声词/口头禅词表挖掘**(快通路封闭集词表的统计来源)。**硬边界**:混合通道语料经源分离后不做控制监督金标签(轻声应声词是分离最先抹掉的成分+伪影串扰教坏自听判别);版权语料只限内部研究用途,商用交付的模型训练数据须可审计,发布走 data card。
6. **合规**:采集即签双通道使用同意;发布走 data card(对金融客户反而是加分项)。

---

## 3. 训练方案

### 3.1 三阶段

| 阶段 | 训练对象 | 任务 | 数据 | 算力 |
|---|---|---|---|---|
| **Stage A 模态对齐** | adapter + 编码器 LoRA(LLM 冻结) | **多任务**:ASR 转写 + 声调序列预测 + 语气/情感分类(λ 各 ~0.1;单一 ASR 目标会让 adapter 只保内容丢韵律) | CS-ASR 数据几百–1000h(现成资产复用) | 大头,4×48G 数天 |
| **Stage B 任务 SFT** | adapter + LLM LoRA | chunk 级因果控制 token(见 3.2);多任务辅助:L = L_intent + λ₁L_tone + λ₂L_LID | §2 全部;单语粤:单语英:CS ≈ **4:3:3**(CS 过采样但单语占多数,防遗忘) | 中 |
| **Stage C 校准/偏好** | 阈值 + 少量偏好优化 | 误停 vs 漏停代价不对称,按行业加权;扫仲裁阈值出 profile | 真实困难样本小集 | 轻 |

LLM 主干全程只动 LoRA,底座(Qwen 系,粤语已验证)中英能力有兜底。粤语书面口语字欠代表是比 CS 更大的短板 → 必要时 Stage A 前加一轮粤文继续预训练。

### 3.2 因果 chunk 标签(最易做错处)

监督必须回答"**给定截至此刻的证据**,正确动作是什么",不是整句听完的事后答案:"唔係——"前 200ms 正确标签可能仍是 ATTENUATE(证据不足),到"唔係啊,我要嘅係——"才升级 CUT。做法:对每条对话在随机截断点生成 chunk 级标签序列(从完整标注按时间投影,禁止未来泄漏)。教会模型"证据不足保守、证据充分果断"的时间行为,并天然配合迟滞策略。

### 3.3 防遗忘门禁

固定单语粤语、单语英语回归测试集,**每次训完必跑(当 CI 用)**,单语退化超阈值打回调配比。这组实验同时是论文二的消融章("CS 训练对单语 duplex 控制的影响 + 配比/adapter 消融")——学术产出与产品调优同一批实验。

### 3.4 快通路训练

单卡量级;应声词检测头 + 声调轮廓特征在种子真集+合成上训;远场/信道鲁棒性在此层解决。算力占比小,重要性不小(应声词漏检一次 = demo 抖一次)。

---

## 4. 评测(评测即产品,一鱼三吃)

- **因果探针 harness**(survey §7 方法论产品化):受控位置注入应声/纠正/旁语/噪声探针;指标:误停率、漏停率、反应延迟 p50/p90、恢复正确性(含"不重复已播内容")。
- 三个用途:① **CS-Duplex-Bench** 公开论文占坑(定义行业量尺);② 自家回归测试;③ 客户验收工具。
- 附加:韵律探针集(§1.5)、单语回归门禁(§3.3)。

---

## 5. Demo 脚本(M1/M2 交付物,融资/获客/论文共用素材)

并排双屏,同一音频喂两系统(左 = server-VAD 级联基线,右 = 同一级联 + 控制器):
1. agent 讲解套餐,用户"嗯嗯…係啊…okok"——左停三次,右流畅讲完;
2. 用户"唔係,我要嘅係下个月 er next month 嘅账单"——右 300ms 内停、从正确内容点接"明白,下个月嘅账单係…";左要么反应慢要么恢复时重复已说内容;
3. 用户转头对家人"帮我攞杯水"——左当成指令,右不动。

三场景一一对应三大卖点。

---

## 6. 部署规格

- 交付:INT4/INT8 量化,gRPC/WebSocket sidecar 服务或进程内库;LiveKit / Vapi / FreeSWITCH(电话)接入件;
- 主目标:单张 4060/4090 跑 8–32 并发路,与客户级联同机;
- 降级模式:纯快通路(VAD + 封闭集应声词)跑 CPU,嵌入式/边缘备选,非主设计目标;
- 每动作日志 + 可调阈值 + 行业 profile = 可审计交付。

---

## 7. 里程碑

| 阶段 | 时间 | 交付 |
|---|---|---|
| **M1** | 第 1–2 月 | 采集协议定稿;种子集 20h;合成 pipeline v1;评测 harness v1;快通路上线,**demo 场景 1 跑通**;韵律探针集建成 |
| **M2** | 第 3–4 月 | Stage A/B 完成,慢通路 + 混语语义判别;**三场景 demo 视频**;**论文一(CS-Duplex-Bench:benchmark + 数据)投出** |
| **M3** | 第 5–8 月 | 量化交付 + LiveKit/FreeSWITCH 接入件;香港客服**外包商**(比银行好进)1–2 家试点;**论文二(控制器模型:韵律旁路/切换 token/因果标签/配比消融)** |

算力画面:8 卡中训练偶尔满负荷数天,平时大部分卡跑 TTS 合成农场 + 评测 harness——数据与评测驱动而非算力驱动,正是 4–8 卡团队对大厂有胜算的战场。

**立即启动(关键路径且互不阻塞)**:①种子数据采集协议(周期最长);②评测 harness(一切度量的基础)。模型不急——快通路用开源组件一周拼出 v0。

---

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 粤语流式 ASR partial 质量差 | 决策通路本就不依赖 ASR(音频直入);纯声学 + 封闭集词表覆盖 ~80% 场景,ASR 只做增强 |
| Stage A 的 ASR 目标冲掉韵律(丢失点②) | Stage A 多任务 + 韵律探针集每阶段检测;备选:独立韵律投影层 |
| 原生大模型吸收控制层能力 | 护城河钉死:电话窄带 + 私有化 + 粤语长尾(大厂 3 年内不优先);同时靠数据飞轮/标准制定/集成深度增厚 |
| 全 CS 数据致单语退化 | 4:3:3 配比 + LoRA(底座冻结)+ 单语回归门禁当 CI |
| 合成数据指纹 | timing 从真集分布采样;真集混入;评测集与合成生成器隔离 |
| 数据合规 | 采集即签双通道同意;data card |

---

## 9. 决策点(2026-09-17 已过一轮 check,结论如下)

1. **慢通路底座 → 已定:Qwen3-1.7B**(不用 2.5 系;thinking 模式必须关 `enable_thinking=False`)。底座是可换件(adapter+LoRA,主干冻结),换底座只需重跑 Stage A/B;若 Qwen3.5 出 2B 级 dense 小模型,摸底后可换。
2. **编码器初始化 → 倾向 Freeze-Omni 编码器**。澄清:FO 的 duplex 能力不在编码器(编码器就是 ASR 训的 chunk 流式声学编码器),单独拿用是标准做法;两边都没粤语(SenseVoice 有粤语但非流式),粤语靠 Stage A 用自有 CS-ASR 数据教,故选择标准=架构匹配度,FO 胜(chunk 流式+为接 LLM adapter 而生+全开源)。M1 做小对照实验(同数据两初始化,韵律探针+粤语 ASR 小测)钉死。
3. **种子集 → 必须真录音,不合成**(种子=timing 分布源+金标签+评测真轨,合成种子=管线空转)。采集协议:远程通话各自本地录音(天然通道分离)>同室双麦;内容=自由闲聊+任务角色扮演(指令埋事件)+**Wizard-of-Oz 子集**(人扮 agent,因为人对人打断时机分布≠人对机);先 10-20h 验证 pipeline 再扩。
4. **Benchmark → 混合三轨,与 MPFD 内容独立但方法论直接迁移**(MPFD 已验证纯合成会被"TTS 假象"毙掉):轨A 受控合成探针(分级难度)、轨B 种子真集留出(永不参训)、轨C 注入协议(真人探针语句 splice 进 agent 对话,20_inject_agent.py 思路+接缝检测照搬)。铁律:评测合成的 TTS/模板与训练合成生成器隔离。
5. **首个客户细分 → 搁置,M3 前再定**。
6. **粤文继续预训练 → 先摸底再决定**(判别模型只需"读懂",达标线低于生成模型)。若做,数据分层:粤文维基(zh-yue)+HKCanCor/CantoMap+Common Voice yue 文本侧;hon9kon9ize 社区 CantoneseLLM 语料+jed351 等 HF 公开集(省清洗);港式论坛文本用合规公开 dump 勿自爬;自有 CS-ASR 粤语转写(量小权重高)。总量 1-5B token 对 1.7B 模型够,4 卡数天。
